<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LastExercise.Views.FillNhanVienPage"
             xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             Title="Fill in for in box">
    <ContentPage.Content>
        <Grid RowDefinitions="1.5*,2*,0.4*">
            <controls:CircleImage Source="{Binding IMG}" Grid.Row="0" HorizontalOptions="Center"/>
            <ScrollView Grid.Row="1">
                <StackLayout>
                    <Entry Placeholder="ID..." Keyboard="Numeric"/>
                    <Entry Placeholder="Name..." Keyboard="Text"/>
                    <DatePicker BindingContext="{Binding Date}"/>
                    <Entry Placeholder="Desc..."/>
                    <Entry Placeholder="Path of image..."/>
                </StackLayout>
            </ScrollView>
            <Button Text="Add" Margin="20,0,20,0" Command="{Binding AddClickCommand}" TextColor="White" BackgroundColor="Aqua"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>